/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['source.unsplash.com'], // Add any additional domains you plan to use
    },
};

export default nextConfig; // Only use module.exports
